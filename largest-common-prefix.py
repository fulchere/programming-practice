def longestCommonPrefix(strs):
    pref = ""
    cur_l = ""
    for i in range(len(strs)):
        if i > len(strs[i])
        if not cur_l:
            cur_l = strs[i]
            continue
        if cur_l != strs[i]:
            return pref
        if i
